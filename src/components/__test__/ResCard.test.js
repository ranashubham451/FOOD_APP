import { render } from "@testing-library/react"
import RestroCard from "../../components/RestroCard"

it("Should render RestroCard Compoent with props data",()=>{
    render(<RestroCard resData={}/>)
})